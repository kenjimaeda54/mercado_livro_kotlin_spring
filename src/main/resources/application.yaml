spring:
  profiles:
    active: dev
  jpa:
    hibernate:
      dll-auto: update
  jackson:
    default-property-inclusion: non_null

jwt:
  secret:  abf9f11d-94c2-4b93-a8c4-870b883a666d
  expiration: 999999

#active: dev
#sempre ue possuir application-dev sera essa configuraçao junto com o application

#posso detimar em edit vai abrir um pop up
#dai em VM options: -Dspring.profiles.active=<ambiente>

#  recomendado
#  spring.jpa.hibernate.ddl-auto=update
#    spring.datasource.url=jdbc:mysql://localhost:3306/mydatabase
#    spring.datasource.username=myuser
#    spring.datasource.password=secret
#    spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
#  spring.jpa.show-sql: true

# default-property-inclusion e para sumir os retornos do tipo null